spring:
    application:
        name: register
    datasource:
        driver-class-name: org.postgresql.Driver
        url: jdbc:postgresql://localhost:5432/register
        username: postgres
        password: qwert
    jpa:
        hibernate.ddl-auto: create-drop
    jackson:
        mapper:
            ACCEPT_CASE_INSENSITIVE_ENUMS: true
        deserialization:
            FAIL_OM_UNKNOWN_PROPERTIES: true
            FAIL_ON_MISSING_CREATOR_PROPERTIES: true

api:
    security:
        token:
            secret: ${JWT_SECRET:secret}